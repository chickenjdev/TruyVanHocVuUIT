import React, { Component } from "react";

import { List, Search } from 'react-bootstrap-icons';

function Title() {
    const { data } = "tool trace log - service";
    return (
        <div class="title-bar" >
            <p>
                Terminal
            </p>
        </div>
    );

}

export default Title;